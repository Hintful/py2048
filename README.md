# py2048
Simple implementation of 2048 game.

---

## How to run

Run `python3 play.py [board_size]` from the root directory. `board_size` is optional; default is 4.

## How to play

Same rule as the original 2048 game - for direction command, `0` is up, `1` is right, `2` is down, and `3` is left.
